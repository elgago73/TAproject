namespace TAproject
{
    public class Student
    {
        public long StudentId;
        public string Name;

        public Student(long studentId, string name)
        {
            StudentId = studentId;
            Name = name;
        }
    }
}